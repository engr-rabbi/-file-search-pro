# -file-search-pro
Professional local file search &amp; indexing desktop app for Windows, built with Electron, Node.js, and SQLite (FTS5). Instant name/wildcard/regex/fuzzy/content search, smart filters, live preview, duplicate finder, disk usage analyzer, OCR search, and a Windows 11-style UI. 100% offline — no cloud, no telemetry, no paid APIs
